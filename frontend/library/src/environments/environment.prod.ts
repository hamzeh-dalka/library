export const environment = {
  production: true,
  // TODO: point this at the deployed API's base URL before shipping a
  // production build (this file wasn't wired up before - it was empty).
  apiUrl: 'https://localhost:44346/api'
};
